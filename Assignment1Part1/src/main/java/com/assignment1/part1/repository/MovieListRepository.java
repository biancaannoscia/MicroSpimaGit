package com.assignment1.part1.repository;

public class MovieListRepository {
//The repository directory contains the JPA based MovieListRepository class that will implement a method finding a list of 
	//movies for a specified viewer. That method will be used by POST method of the MovieListController 
	//class (described above) to persist a newly created movie to H2 embedded database.
}
